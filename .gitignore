# Local tunnel binary (~53 MB) and its log — downloaded tooling, not source.
cloudflared.exe
cloudflared.log

# Build/packaging output
SafeWalk-app.zip

# OS noise
Thumbs.db
.DS_Store

# Scratch copy of the test suite, staged only to run it in a browser.
safewalk-app/_tmp_test.js

# Scratch copy of the permission suite, staged only to run it in a browser.
safewalk-app/_tmp_perm.js
safewalk-app/_tmp_rep.sql
